# Activity logs

Use this directory only for short-lived, local activity records when a task spans multiple sessions. Keep entries factual and omit secrets, credentials, personal data, and full machine-specific paths.

Log files are ignored by default. Do not use them as a substitute for issue descriptions, pull request details, or durable repository documentation.
